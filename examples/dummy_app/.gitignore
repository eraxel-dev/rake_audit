# SQLite databases — regenerated by `bin/rails db:setup`.
/db/*.sqlite3
/db/*.sqlite3-*

# Logs and temp files.
/log/*
/tmp/*
!/log/.keep
!/tmp/.keep

# Bundler local config.
/.bundle

# Local bundle gem cache (do not commit installed gems).
/vendor/bundle
